[project]
name = "img2pdf"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.3.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.22",
]
